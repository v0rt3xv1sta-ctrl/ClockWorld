/*
 * net.js — multiplayer client (WebSocket). Implemented in Phase 2; this
 * placeholder keeps the script tag valid until then.
 */
(function (global) {
  "use strict";
  global.Net = global.Net || { available: false };
})(typeof window !== "undefined" ? window : globalThis);
